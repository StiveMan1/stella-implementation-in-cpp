language core;

fn list_construct(n : Nat) -> [Nat]{
    return [n, n, n];
}

fn main(n : Nat) -> Nat {
   return let m = list_construct(n) in
   if List::isempty(m) then (
       List::head(m) + List::tail(m)
   ) else 0;
}
